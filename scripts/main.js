// /src/main/resources/static/scripts/main.js

import App from './core/App.js';  // Import the main App class

// Initialize the application when the DOM is fully loaded
document.addEventListener('DOMContentLoaded', () => {
    const app = new App();
});
